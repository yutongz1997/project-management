{
  "name": "project-management",
  "version": "0.1.0",
  "description": "A tiny project management system with a React frontend and an Express backend.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yutong Zhang",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
